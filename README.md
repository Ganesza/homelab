# homelab
# Homelab  Public notes from a Rocky Linux VPS (Google Cloud).  ## Live - HTTP: http://34.116.177.8/  ## Done - [x] nginx on port 80, reachable from the internet - [x] firewall (host + GCP)  ## Next - systemd service that survives reboot - SSH keys, backups
# Homelab

Public notes from a Rocky Linux VPS (Google Cloud).


## Live
- Simple nginx page on a Rocky Linux VPS (Google Cloud)
- HTTP reachable from the internet (lab only, not a product)
